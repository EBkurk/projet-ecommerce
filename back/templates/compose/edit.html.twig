{% extends 'base.html.twig' %}

{% block title %}Edit Compose{% endblock %}

{% block body %}
    <h1>Edit Compose</h1>

    {{ include('compose/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_compose_index') }}">retour a la liste</a>

    {{ include('compose/_delete_form.html.twig') }}
{% endblock %}
