{% extends 'base.html.twig' %}

{% block title %}Compose{% endblock %}

{% block body %}
    <h1>Compose</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compose.id }}</td>
                <td>{{ compose.materiaux }}</td>
                <td>{{ compose.produit }}</td>
            </tr>
            <tr>
                <th>Materiaux Id</th>
                <td>{{ compose.materiaux }}</td>
            </tr>
            <tr>
                <th>Produit Id</th>
                <td>{{ compose.produit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compose_index') }}">retour a la liste</a>

    <a href="{{ path('app_compose_edit', {'id': compose.id}) }}">edit</a>

    {{ include('compose/_delete_form.html.twig') }}
{% endblock %}
