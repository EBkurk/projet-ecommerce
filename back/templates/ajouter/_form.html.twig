{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Créer') }}</button><br><br>
    <a href="{{ path('app_produit_new') }}">Créer un produit</a><br>
    <a href="{{ path('app_commande_new') }}">Créer une commande</a><br><br>
{{ form_end(form) }}
