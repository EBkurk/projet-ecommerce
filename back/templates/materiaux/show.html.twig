{% extends 'base.html.twig' %}

{% block title %}Materiaux{% endblock %}

{% block body %}
    <h1>Materiaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiaux.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ materiaux.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_materiaux_index') }}">retour a la liste</a>

    <a href="{{ path('app_materiaux_edit', {'id': materiaux.id}) }}">edit</a>

    {{ include('materiaux/_delete_form.html.twig') }}
{% endblock %}
